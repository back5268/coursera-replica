import {bucket, uploadFileToFirebase} from "@lib/firebase";
const filePath = 'src/public/test.png';
const uploadTo = 'images/test.png';

const test = async () => {
    // const a = await uploadFileToFirebase({ filePath, uploadTo })
    // console.log(a)
};

test();
